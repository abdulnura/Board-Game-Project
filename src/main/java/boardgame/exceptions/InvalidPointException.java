package boardgame.exceptions;

/**
 * Checks if the coordinates are within the board size
 * if not it will throw the InvalidPointException and ask user to try again
 */

public class InvalidPointException extends AbstractException {
}
